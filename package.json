{
  "name": "dashteam",
  "version": "1.0.0",
  "description": "application de gestion d'employées",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel api client",
    "api": "npm --prefix ./api start",
    "client": "npm --prefix ./client run dev",
    "build": "npm-run-all build:api build:client",
    "build:api": "npm --prefix ./api run build",
    "build:client": "npm --prefix ./client run build"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "author": "FACKOP GROUPE",
  "license": "ISC"
}
